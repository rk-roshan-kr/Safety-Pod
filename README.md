# Safety-Pod
SafetyPod is a campus safety prototype that helps students move safely through walking groups (“pods”), live map routing, and an SOS alert system. Users can join or create pods, share routes, add emergency contacts, and trigger alerts to contacts and campus security with one tap.
